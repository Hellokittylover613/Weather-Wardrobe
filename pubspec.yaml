name: weather_wardrobe
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.2.0
  geolocator: ^14.0.2
  google_fonts: ^6.2.1
  http: ^1.6.0
  flutter_dotenv: ^5.2.1
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env